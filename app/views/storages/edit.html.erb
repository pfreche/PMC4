<h1>Storage</h1>
<p id="notice"><%= notice %></p>
<%= render 'form' %>



  <% if false%>
  <%= link_to 'Detect Files and Folders', detectFaFs_storage_path(@storage) %>
<p>
  <%= link_to 'Detect Folders', detectfolders_storage_path(@storage) %>
  <%= link_to 'Detect Files', detectfiles_storage_path(@storage) %>
   <%= link_to 'Make Thumbnails', make_thumbnails_storage_path(@storage) %>


  <%end%>
    <%= form_tag touchMfiles_storage_path(@storage), method: "post"  do %>

       <%= submit_tag("Set Dates of Mfiles in this Storage", class: "buttonForm") %>
    <% end %>
