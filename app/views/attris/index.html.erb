<h1>Attributes
</h1>

<% if @selectedAttributes %>
 <div class="lbl">
  <strong>Selected Attributes:</strong>
  </div>

<%      @selectedAttributes.each do |attri| %>    

<span class='attributenew'> <%= attri.name %> 
        <%= link_to 'X', attris_path+"?id_remove="+attri.id.to_s, method: :get%>
         </span>

 <% end %>
<%= link_to material_icon.delete.css_class('md-18'), attris_path+"?reset=1"%>
 <% end %>
 <div class="valuepair">
 <div class="lbl">
  <strong>Show selected Mfiles:</strong>
  </div>
 <%= link_to "<As List>", mfiles_path+"?typ=attribute", method: :get %>
 <%= link_to "<As Thumbs>", thumbs_mfiles_path+"?typ=attribute", method: :get %>
 </div>
<hr>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Agroup</th>
      <th>Id sort</th>
      <th>Parent</th>
      <th>Keycode</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @attris.each do |attri| %>
      <tr>
        <td><%= attri.name %></td>
        <td><%= attri.agroup.name %></td>
        <td><%= attri.id_sort %></td>
        <td><%= attri.parent_id %></td>
        <td><%= attri.keycode %></td>
        <td><%= link_to 'Show', attri %></td>
        <td><%= link_to 'Edit', edit_attri_path(attri) %></td>
        <td><%= link_to 'Destroy', attri, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        
        		<td> 
         <td> <%= link_to @numbers[attri.id], attris_path+"?id_add="+attri.id.to_s, method: :get %>
          <td> <%= link_to @numbersall[attri.id], attris_path+"?id_select="+attri.id.to_s, method: :get %>
      <td> <%= link_to @numbers[attri.id], find_attris2_path(attri.id), method: :get %>


      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Attri', new_attri_path %>
