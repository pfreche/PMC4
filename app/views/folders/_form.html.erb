<%= form_for(@folder) do |f| %>
  <% if @folder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@folder.errors.count, "error") %> prohibited this folder from being saved:</h2>

      <ul>
      <% @folder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class= "fieldbox">
  <div class="field">
    <%= f.label :storage_id %>
    <%= f.number_field :storage_id,  size: 100 %>
  </div>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title,  size: 90 %>
  </div>
  <div class="field">
    <%= f.label :mpath %>
    <%= f.text_field :mpath,  size: 90 %>
  </div>
  <div class="field">
    <%= f.label :lfolder %>
    <%= f.text_field :lfolder,  size: 90 %>
  </div>
  <div class="field">
    <%= f.label :mfile_id %>
    <%= f.number_field :mfile_id,  size: 100 %>
  </div>
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
